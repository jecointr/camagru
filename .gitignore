# --- 1. Sécurité & Environnement ---
# Le fichier .env contient tes mots de passe BDD, ne jamais le commiter !
.env

# Si tu crées des fichiers de config locaux pour tester
config/config.local.php

# --- 2. Contenu Utilisateur (Uploads) ---
# On ignore toutes les images dans le dossier uploads...
public/uploads/*
# ... Sauf le fichier .gitkeep (pour conserver l'arborescence du dossier)
!public/uploads/.gitkeep

# --- 3. Logs & Débogage ---
*.log
error_log

# --- 4. Système d'exploitation & IDE ---
# Mac
.DS_Store
# Windows
Thumbs.db
# VS Code
.vscode/
# JetBrains (PhpStorm)
.idea/

# --- 5. Dépendances (Optionnel) ---
# Si tu utilises Composer à l'avenir
/vendor/
